# Othie AI website

Reserved npm workspace: `@othie/website`. The website is not implemented yet.

Build the landing, pricing, and download pages from [the website brief](../../docs/website-build-plan.md).
Use this directory for the app's source, framework configuration, and dependencies.
Install dependencies from the repository root with `npm install <dependency> --workspace=@othie/website`.
Use the shared root lockfile; do not initialize a nested Git repository.
Add real build/dev/typecheck scripts when the app exists. Preserve the other workspaces.
